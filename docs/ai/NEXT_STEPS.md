# Next Steps — CrewAnywhere

From `../../CURRENT_STATE.md` and `../../TASKS.md` (keep in sync; brain updates confirm-first).

## Near-term
- Build domain features inside their modules, in lifecycle order (Event -> Job -> Proposal -> Assignment -> Shift -> Payment), each honoring the atomic transition engine.

## Doc-hygiene (from this bootstrap)
- Verify `docs/ai` index links resolve to the real `docs/` files.
- Mark intended-vs-implemented status in each domain's docs.
- Clean remaining brain-file formatting (`CURRENT_STATE.md`, `DECISIONS.md`, `TASKS.md`) — confirm-first before editing.
